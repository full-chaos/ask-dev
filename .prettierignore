# Dependencies and build output
node_modules
dist
coverage
playwright-report
test-results

# Exact copied ACR wire artifacts stay byte-identical to the pinned acr source.
# The sync script owns their formatting; Prettier must never rewrite them.
src/contracts/

# Corpus of record baseline sweep: must stay byte-identical to the sha it was
# generated at (see corpus/README.md). Prettier must never rewrite it.
corpus/baseline-20260905-sweep1.json

pnpm-lock.yaml
.env*

# Scratch the code-review tooling drops in the worktree root: never
# committed (kept untracked out of the tree), and not ours to format.
.codex-review-context.md
prompt.md
